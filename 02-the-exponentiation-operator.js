// Math.pow works, but harder to read

const resultPow = Math.pow(2, 2)

console.log(resultPow) // 4

// Exponentiation Operator easier to read

const resultExp = 2 ** 2

console.log(resultExp) // 4
